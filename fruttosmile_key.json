import os
import json

# Ключ берём из переменной окружения GOOGLE_KEY_JSON (на Render добавишь позже)
GOOGLE_KEY_JSON = os.getenv("GOOGLE_KEY_JSON")
if GOOGLE_KEY_JSON is None:
    raise ValueError("GOOGLE_KEY_JSON not set in environment variables")

creds_dict = json.loads(GOOGLE_KEY_JSON)
creds = ServiceAccountCredentials.from_json_keyfile_dict(creds_dict, scope)
